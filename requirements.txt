# Core HTTP and web scraping
requests>=2.28.0
urllib3>=1.26.0
beautifulsoup4>=4.11.0

# Browser automation
selenium>=4.15.0
webdriver-manager>=4.0.0

# Anki deck generation
genanki>=0.13.0

# Image processing for portrait detection
Pillow>=10.0.0

# Progress bars and user interface
tqdm>=4.65.0

# GUI file dialogs (tkinter is built-in with Python)
# Note: tkinter comes with Python by default on most systems
# The setup script automatically installs GUI support:
#   - macOS: brew install python-tk (automatic)
#   - Linux: sudo apt install python3-tk (with prompt)
#   - Windows: included with Python (usually works by default)

# Environment and configuration
python-dotenv>=1.0.0